<% @post.each do |post| %>
    <h1> <%= post.title %><br> </h1>
    <p> <%= post.content %><br> </p>
    <%= post.created_at.strftime("%B %d %Y, %l:%M%P") %>
    <hr>
<% end %>
<%= link_to "<<Back", root_path %>